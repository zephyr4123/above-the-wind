# 本机 skill 脚手架：.claude/ 内是指向本机绝对路径的符号链接，CLAUDE.md 为项目级 Claude 配置。
# 二者均为本机专属、不该进版本库（别的机器上缺失会被 Claude 静默跳过，无副作用）。
.claude/
CLAUDE.md

# macOS
.DS_Store

# Python
.venv/
__pycache__/
*.pyc

# 采集的原始数据(可由 harvest/ 脚本复现,大体量不入库;curated 子集另议)
# 注意锚定根目录:只忽略根 data/,不能误伤 web/src/data/(peaks.ts / citations.ts 是源码)
/data/

# 浏览器验证的临时产物(Playwright MCP 截图/快照/控制台日志)
.playwright-mcp/
/atw-*.png
/atw33-*.png
/chron-*.png
